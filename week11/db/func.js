import pool from "./db.js";

async function getAll() {
    try {
        const table = await pool.execute('')

    } catch (error) {
        console.error({ error });
    }   
}
async function creatTable() {
    try {
        const table = await pool.execute('') 
    } catch (error) {
        console.error({ error });
    }   
}
async function creatTable() {
    try {
        const table = await pool.execute('') 
    } catch (error) {
        console.error({ error });
    }   
}
async function creatTable() {
    try {
        const table = await pool.execute('') 
    } catch (error) {
        console.error({ error });
    }   
}